# Assembled read data

This directory is intentionally kept out of Git.

Expected files:

- Assembled FASTQ files, named like `NovABWGB16SAB27963-10-5.assembled.fastq.gz`.
- The current local dataset contains 163 `.fastq.gz` files, about 516 MB total.

These files are derived from the raw sequencing reads and may be regenerated by the preprocessing workflow.

