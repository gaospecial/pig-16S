# DADA2 filtered reads

This directory is intentionally kept out of Git except for lightweight summary files.

Expected large files:

- Filtered FASTQ files, named like `NovABWGB16SAB27963-10-5_F_filt.fq.gz`.
- The current local dataset contains 163 filtered `.fq.gz` files, about 513 MB total.

Tracked files:

- `filter-stats.csv` records filtering statistics and is small enough to keep in Git.

