# Raw sequencing data

This directory is intentionally kept out of Git.

Expected files:

- Paired-end raw 16S reads, named like `NovABWGB16SAB27963-10-5_R1.fq.gz` and `NovABWGB16SAB27963-10-5_R2.fq.gz`.
- The current local dataset contains 326 `.fq.gz` files, about 1.1 GB total.

These files are the inputs for the preprocessing workflow in `Preprocess/amplicon-dada2.qmd`.

